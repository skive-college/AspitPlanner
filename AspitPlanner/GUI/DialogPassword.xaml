<Window x:Class="AspitPlanner.GUI.DialogPassword"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AspitPlanner.GUI"
        mc:Ignorable="d"
        Title="Nyt Password" Height="160" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Text="Opret nyt password" />
        <TextBlock Grid.Column="0" Grid.Row="1" Text="Nyt Password" />
        <PasswordBox Grid.Column="1" Grid.Row="1"  x:Name="txtPassword" />
        <Button Style="{StaticResource standartButton}" Height="25" Grid.Column="1" Grid.Row="2"  x:Name="cmdSave" Content="Gem" Click="CmdSave_Click"/>

    </Grid>
</Window>
