<Window x:Class="AspitPlanner.GUI.LoginGUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AspitPlanner.GUI"
        mc:Ignorable="d"
        Title="LoginGUI" Height="150" Width="300">
    <StackPanel Style="{StaticResource StackPanel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="Navn :"/>
        <TextBlock Grid.Column="0" Grid.Row="1" Text="Kodeord :"/>
        <TextBox Grid.Column="1" Grid.Row="0" x:Name="txtName"/>
        <PasswordBox Grid.Column="1" Grid.Row="1" x:Name="txtPassword"/>
        <Button Grid.Column="1" Grid.Row="3" x:Name="cmdLogin" Style="{StaticResource standartButton}" Content="Log Ind"/>
    </Grid>
    </StackPanel>
</Window>
